"
create external table USER_BASE (
id STRING,
pack STRING,
vip_level STRING,
ol_days INT
)
row format delimited fields terminated by '\t';
"

"
create external table USER_CALL_DETAIL (
id STRING,
time BIGINT,
out BOOLEAN,
duration INT,
location STRING
)
row format delimited fields terminated by '\t';
"

"
create external table USER_TEXT_DETAIL (
id STRING,
time BIGINT,
out BOOLEAN,
location STRING,
content STRING
)
row format delimited fields terminated by '\t';
"

"
create external table USER_NET_DETAIL (
id STRING,
time BIGINT,
location STRING,
content STRING
)
row format delimited fields terminated by '\t';
"

"
create external table USER_CONSUME_DETAIL (
id STRING,
time BIGINT,
consume MAP<STRING,FLOAT>
)
row format delimited fields terminated by '\t' COLLECTION ITEMS TERMINATED BY ',' MAP KEYS TERMINATED BY ':';
"

"
create table CALL_RECORD_TBL (
from_id STRING,
to_id STRING,
time BIGINT,
duration INT,
location STRING
)
row format delimited fields terminated by '\t';
"

load data local inpath '/home/hadoop/tmpdir/call_detail' into table call_record;